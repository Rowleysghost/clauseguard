export const metadata = {
  title: "ClauseGuard",
  description: "AI-powered P2P trade escrow on GenLayer Bradbury Testnet",
};
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
