[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers
markers =
    no_invariant: this test deliberately leaves the accounting inconsistent; skip the autouse invariant check
